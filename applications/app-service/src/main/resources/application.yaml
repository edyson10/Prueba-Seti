server:
  port: 8081
spring:
  application:
    name: "franquicias-back"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    active: "local"
  data:
    mongodb:
      uri: "mongodb://mongo:GaRMzFsFweUiaeWVuEeQwZmeltRGLYce@shinkansen.proxy.rlwy.net:41798"
      database: "mongo"
      auto-index-creation: true
  main:
    allow-bean-definition-overriding: true
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"
